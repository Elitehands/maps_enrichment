# Path to the folder where geographic output files (JSON, GeoJSON, etc.) will be saved.
GEODATA_PATH=out/geodata.json

# Path to your CSV file that contains locations or coordinates.
LOCATIONS_CSV_PATH=data/locations.csv

# Path to your Excel file with location information (if used instead of CSV).
LOCATIONS_EXCEL_PATH=data/locations.ods

# Google API key used for reverse geocoding, Places API, etc.
GOOGLE_API_KEY=YOUR_API_KEY_HERE
